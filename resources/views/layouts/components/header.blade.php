<div class="jumbotron jumbotron-fluid">
    <div class="container">
        <h1>{{$title}}</h1>
        @if(isset($subtitle))
            <p class="lead">{{$subtitle}}</p>
        @endif
    </div>
</div>